<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:views="clr-namespace:NRKLastNed.Mac.Views"
        mc:Ignorable="d" d:DesignWidth="500" d:DesignHeight="350"
        x:Class="NRKLastNed.Mac.Views.AboutWindow"
        x:DataType="views:AboutWindow"
        Title="Om NRK Nedlaster GUI" Height="360" Width="500"
        CanResize="False" WindowStartupLocation="CenterOwner"
        Background="{DynamicResource BgDark}"
        Foreground="{DynamicResource TextColor}">

	<StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Spacing="12" Margin="20">

		<TextBlock Text="NRK Last Ned"
                   FontSize="24"
                   FontWeight="Bold"
                   Foreground="{DynamicResource AccentColor}"
                   HorizontalAlignment="Center"/>

		<TextBlock Text="En enkel nedlaster for NRK TV og Radio"
                   FontStyle="Italic"
                   HorizontalAlignment="Center"/>

		<StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Spacing="6">
			<TextBlock Text="Versjon:" FontWeight="Bold"/>
			<TextBlock Text="{Binding VersionText}"/>
			<TextBlock Text="•" Foreground="{DynamicResource TextMuted}"/>
			<TextBlock Text="Utgitt:" FontWeight="Bold"/>
			<TextBlock Text="{Binding ReleaseDateText}"/>
		</StackPanel>

		<TextBlock Text="Utviklet av Emigrante"
                   FontSize="12"
                   Foreground="{DynamicResource TextMuted}"
                   HorizontalAlignment="Center"/>

		<Button Content="OK"
                Click="Ok_Click"
                HorizontalAlignment="Center"
                Width="100"
                Margin="0,10,0,0"
                ToolTip.Tip="Lukk dette vinduet"/>
	</StackPanel>
</Window>