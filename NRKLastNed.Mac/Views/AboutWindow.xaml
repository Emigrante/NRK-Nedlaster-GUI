<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d" d:DesignWidth="400" d:DesignHeight="250"
        x:Class="NRKLastNed.Mac.Views.AboutWindow"
        Title="Om NRK Last Ned"
        Width="400" Height="250"
        WindowStartupLocation="CenterOwner"
        CanResize="False"
        Background="{DynamicResource BgDark}"
        Foreground="{DynamicResource TextColor}">

	<StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Spacing="15" Margin="20">

		<TextBlock Text="NRK Last Ned"
                   FontSize="24"
                   FontWeight="Bold"
                   Foreground="{DynamicResource AccentColor}"
                   HorizontalAlignment="Center"/>

		<TextBlock Text="En enkel nedlaster for NRK TV og Radio"
                   FontStyle="Italic"
                   HorizontalAlignment="Center"/>

		<StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Spacing="5">
			<TextBlock Text="Versjon:" FontWeight="Bold"/>
			<TextBlock Text="1.0.7"/>
		</StackPanel>

		<TextBlock Text="Utviklet av Emigrante"
                   FontSize="12"
                   Foreground="{DynamicResource TextMuted}"
                   HorizontalAlignment="Center"/>

		<Button Content="OK"
                Click="Ok_Click"
                HorizontalAlignment="Center"
                Width="100"
                Margin="0,10,0,0"/>
	</StackPanel>
</Window>