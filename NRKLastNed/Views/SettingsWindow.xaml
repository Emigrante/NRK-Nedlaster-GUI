<Window x:Class="NRKLastNed.Views.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Innstillinger" Height="650" Width="700"
        MinHeight="500" MinWidth="600"
        Background="{DynamicResource BgDark}"
        Foreground="{DynamicResource TextColor}"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResize">

    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Margin="0,0,0,20">
                <TextBlock Text="INNSTILLINGER" FontSize="20" FontWeight="Bold" Foreground="{DynamicResource AccentColor}" VerticalAlignment="Center"/>
                <Button Content="Lukk" Click="Close_Click" Width="80" HorizontalAlignment="Right" ToolTip="Lukk innstillinger"/>
            </Grid>

            <!-- TV/Radio Nedlastingsmapper -->
            <GroupBox Grid.Row="1" Header="Nedlastingsmapper" Foreground="{DynamicResource AccentColor}" BorderBrush="{DynamicResource BorderColor}" Margin="0,0,0,15">
                <StackPanel Margin="10">
                    <!-- Checkbox for delt mappe -->
                    <CheckBox x:Name="chkUseSameFolder" Content="Bruk samme mappe for både TV og Radio" 
                              Foreground="{DynamicResource TextColor}" Margin="0,0,0,15" 
                              Checked="LiveSave_Trigger" Unchecked="LiveSave_Trigger"/>

                    <!-- TV-mappe -->
                    <TextBlock Text="TV-mappe:" Margin="0,0,0,5"/>
                    <Grid Margin="0,0,0,15">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBox x:Name="txtTvOutput" IsReadOnly="True" TextChanged="LiveSave_Trigger"/>
                        <Button Grid.Column="1" Content="..." Click="BrowseTvOutput_Click" Margin="10,0,0,0" Width="40" ToolTip="Bla gjennom og velg lagringsmappe for TV-programmer"/>
                    </Grid>

                    <!-- Radio-mappe (kun synlig når ikke delt mappe) -->
                    <TextBlock Text="Radio-mappe:" Margin="0,0,0,5" x:Name="lblRadioOutput"/>
                    <Grid Margin="0,0,0,15" x:Name="gridRadioOutput">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBox x:Name="txtRadioOutput" IsReadOnly="True" TextChanged="LiveSave_Trigger"/>
                        <Button Grid.Column="1" Content="..." Click="BrowseRadioOutput_Click" Margin="10,0,0,0" Width="40" ToolTip="Bla gjennom og velg lagringsmappe for radioprogrammer"/>
                    </Grid>
                </StackPanel>
            </GroupBox>

            <GroupBox Grid.Row="2" Header="Midlertidige filer" Foreground="{DynamicResource AccentColor}" BorderBrush="{DynamicResource BorderColor}" Margin="0,0,0,15">
                <StackPanel Margin="10">
                    <CheckBox x:Name="chkUseSystemTemp" Content="Bruk standard Windows temp mappe" 
                              Foreground="{DynamicResource TextColor}" Margin="0,0,0,10" 
                              Checked="LiveSave_Trigger" Unchecked="LiveSave_Trigger"/>

                    <Grid x:Name="pnlCustomTemp" IsEnabled="False">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBox x:Name="txtTemp" IsReadOnly="True" Text="Velg mappe for temp filer..." TextChanged="LiveSave_Trigger"/>
                        <Button Grid.Column="1" Content="..." Click="BrowseTemp_Click" Margin="10,0,0,0" Width="40" ToolTip="Bla gjennom og velg mappe for midlertidige filer"/>
                    </Grid>
                </StackPanel>
            </GroupBox>

            <GroupBox Grid.Row="3" Header="Preferanser" Foreground="{DynamicResource AccentColor}" BorderBrush="{DynamicResource BorderColor}" Margin="0,0,0,15">
                <Grid Margin="10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="20"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0">
                        <TextBlock Text="Standard oppløsning:" Margin="0,0,0,5"/>
                        <ComboBox x:Name="cmbResolution" SelectionChanged="LiveSave_Trigger"/>
                    </StackPanel>

                    <StackPanel Grid.Column="2">
                        <TextBlock Text="Fargetema:" Margin="0,0,0,5"/>
                        <ComboBox x:Name="cmbTheme" SelectionChanged="CmbTheme_SelectionChanged"/>
                    </StackPanel>
                </Grid>
            </GroupBox>

            <GroupBox Grid.Row="4" Header="Verktøy og Oppdateringer" Foreground="{DynamicResource AccentColor}" BorderBrush="{DynamicResource BorderColor}" Margin="0,0,0,15">
                <StackPanel Margin="10">

                    <Grid Margin="0,0,0,15">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel>
                            <TextBlock Text="Programversjon:" FontWeight="Bold"/>
                            <TextBlock x:Name="lblAppVersion" Text="Sjekker..." Foreground="{DynamicResource TextMuted}"/>
                        </StackPanel>
                        <Button x:Name="btnAppUpdate" Grid.Column="1" Content="Oppdater" Click="AppUpdate_Click" Width="100" IsEnabled="False" ToolTip="Søk etter og installer nyeste versjon av NRK Nedlaster GUI"/>
                    </Grid>

                    <Separator Background="{DynamicResource BorderColor}" Margin="0,0,0,15"/>

                    <Grid Margin="0,0,0,10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel>
                            <TextBlock Text="yt-dlp status:" FontWeight="Bold"/>
                            <TextBlock x:Name="lblYtDlpVersion" Text="Sjekker versjon..." Foreground="{DynamicResource TextMuted}"/>
                        </StackPanel>
                        <Button x:Name="btnYtDlpUpdate" Grid.Column="1" Content="Oppdater yt-dlp" Click="UpdateYtDlp_Click" Width="140" IsEnabled="False" ToolTip="Søk etter og last ned nyeste versjon av yt-dlp"/>
                    </Grid>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel>
                            <TextBlock Text="FFmpeg status:" FontWeight="Bold"/>
                            <TextBlock x:Name="lblFfmpegVersion" Text="Sjekker..." Foreground="{DynamicResource TextMuted}"/>
                        </StackPanel>
                        <Button x:Name="btnFfmpegUpdate" Grid.Column="1" Content="Oppdater FFmpeg" Click="GetFfmpeg_Click" Width="140" IsEnabled="False" ToolTip="Søk etter og last ned nyeste versjon av FFmpeg"/>
                    </Grid>
                </StackPanel>
            </GroupBox>

            <GroupBox Grid.Row="5" Header="Logging" Foreground="{DynamicResource AccentColor}" BorderBrush="{DynamicResource BorderColor}" Margin="0,0,0,15">
                <StackPanel Margin="10">
                    <CheckBox x:Name="chkEnableLog" Content="Aktiver Logging til fil" Foreground="{DynamicResource TextColor}" Margin="0,0,0,10" Checked="LiveSave_Trigger" Unchecked="LiveSave_Trigger"/>

                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Loggnivå:" VerticalAlignment="Center" Margin="0,0,10,0"/>
                        <ComboBox x:Name="cmbLogLevel" Width="150" SelectionChanged="LiveSave_Trigger"/>
                    </StackPanel>

                    <TextBlock Text="Loggfiler lagres i mappen 'Logs' der programmet kjører." Foreground="{DynamicResource TextMuted}" FontSize="10" Margin="0,5,0,0"/>
                </StackPanel>
            </GroupBox>

            <TextBlock Grid.Row="7" Text="INFO: yt-dlp.exe og ffmpeg.exe må ligge i 'Tools' mappen." Foreground="{DynamicResource TextMuted}" FontStyle="Italic" Margin="0,0,0,20"/>
        </Grid>
    </ScrollViewer>
</Window>